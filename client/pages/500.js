import BaseLayout from '@components/BaseLayout';

export default function Custom500() {
  return (
    <BaseLayout title="500 - Server-side error occurred">
      <section>
        <h1>500 - Server-side error occurred</h1>
      </section>
    </BaseLayout>
  );
}
